package com.example.dailydo.enums;

public enum ErrorCode {
    VALIDATION_ERROR,
    EMAIL_DUPLICATE,
    PASSWORD_MISMATCH,
    RESOURCE_NOT_FOUND,
    TOKEN_INVALID,
    UNAUTHORIZED,
    BAD_REQUEST,
    INTERNAL_ERROR
}
